{
    "name": "wordpressistic/g2a-booking-engine",
    "description": "Dev tooling only: unit tests and CI for the G2A Booking Engine WordPress plugin. The plugin itself has no Composer runtime dependencies.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "scripts": {
        "test": "phpunit -c phpunit.xml"
    },
    "config": {
        "sort-packages": true,
        "optimize-autoloader": true
    }
}
