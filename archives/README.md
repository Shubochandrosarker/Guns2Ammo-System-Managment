# Archives

Superseded material, retained for reference and traceability. **Nothing in this
folder is installed, executed, or part of the running system.**

| Path | Contents |
| --- | --- |
| `plugins/guns2ammo-waiver-manager/` | Retired plugin, superseded by Memberistic's waiver system. It shows a retirement notice and hard-returns before registering any hook, so it contributes no behaviour even if activated. Do not install. |
| `releases-legacy/` | Historical release ZIPs (26 builds) predating the `dist/` layout. Kept for rollback reference. |
| `docs/` | Point-in-time audits, incident reports and dated migration/reconciliation notes. Accurate as of their dates; not maintained. |
| `release-notes/` | Superseded system release notes (2.1.0, 2.5.0). |
| `assets/` | Source documents and design assets (DOCX, PDF, image ZIP). |

Current code lives in `plugins/`, `themes/` and `apps/`. Current builds are in
`dist/`. Current documentation is in `docs/`.
