# Client Requirements Gap Matrix

Every line item from the client's July 14 message, independently re-verified against current source this session (not copied from prior docs — see divergence notes where this audit's finding differs from or sharpens a prior claim).

| # | Requirement | Current status | Evidence | Missing part | Severity | Recommended action | Acceptance test |
|---|---|---|---|---|---|---|---|
| 1 | Cancelling a membership doesn't cancel the Stripe subscription | **Confirmed code defect** | `class-memberships-repository.php:535,546` writes local status, then fires hook; `class-stripe-service.php:212-262` calls Stripe and on failure only logs an Activity entry — no revert, no distinct failure state | Atomic/compensating workflow; visible `cancel_failed` state; cross-membership failure queue | **Critical** | Reorder to attempt-remote-first (or add compensating rollback + `requires_review` status); add admin "failed cancellations" list | Cancel a membership with Stripe API key temporarily invalidated in a staging environment → confirm status shows `requires_review`, not `cancelled`, and appears in a staff-visible queue |
| 2 | Address on booking/membership/waiver/chatbot emails is wrong | **Partially fixed in code; live verification required** | `guns2ammo/inc/business-info.php` (`g2a_biz()`) is the real single source of truth; `g2a-booking-engine/includes/helpers/functions.php:97-104` (`g2ab_business_address()`) already treats the literal placeholder `"Mesa, Arizona"` as "unconfigured" and falls back to `g2a_biz_addr_line()` | This self-heals only the ONE known placeholder string. If production's `g2ab_business_address` option holds any OTHER previously-typed wrong value (not exactly `"Mesa, Arizona"`), the fallback will not trigger. No confirmation the fix has been deployed to production, and no delivered-email test on file. | High | Live-check the actual saved option value in production; send one real test email per surface (booking confirmation, membership welcome/cancel, waiver, AI autoreply) and visually confirm the address | `wp option get g2ab_business_address` on production; trigger one email from each of the 4 surfaces named by the client and inspect the rendered address |
| 3 | No calendar for classes on the backend | **Confirmed code defect (root cause identified, more precise than prior docs)** | Calendar code is complete and defensively engineered: `g2a-booking-engine/includes/admin/class-calendar.php` (FullCalendar bundled locally specifically to avoid CDN-block failures, `manage_g2ab_bookings` capability gate). But `includes/class-activator.php:104-141` only grants that capability inside `register_activation_hook`, and the client's own confirmed deploy process (`docs/CLIENT~1.MD` item 13: direct zip overwrite, no staging) never re-fires that hook. | An idempotent, version-triggered capability-provisioning routine (runs on every upgrade, not just first activation) | **Critical** | Immediate unblock: have the client (or staff with FTP/wp-admin access) **deactivate then reactivate** the Booking Engine plugin once — this safely re-runs `register_roles_and_caps()` without touching booking data. Permanent fix: add an `admin_init`/`plugins_loaded` version-check that re-provisions capabilities on every upgrade. | After reactivation, log in as the affected staff account and confirm "G2A Booking → Calendar" is visible in the admin menu |
| 4, 5 | Page corrections not done; content control not handed off; small per-page issues remain | **Confirmed missing — operational/training gap, not a code gap** | Theme + G2A Theme Control (only 472 LOC) expose content through normal WP editing screens; no restricted content-editor role found anywhere in the repo; no page-by-page correction register artifact in-repo; no recorded-walkthrough asset in-repo | A capability-scoped `content_manager` role; a real correction-tracking artifact (not a doc that goes stale); a recorded walkthrough | High (client trust/handoff risk, not technical risk) | Build the role + register (see `10-COUNTER-WORKFLOW-AND-STAFF-HANDOFF.md`); inventory every public page's editable vs. hardcoded fields | Client (using the new restricted role, not an admin account) successfully edits and republishes one real page correction without developer help |
| 6 | Lipsey's API attempted, same result; product categories didn't carry over | **Confirmed code defects — three distinct, independently evidenced bugs** | (a) `WholesalerRepository::findByCode()` — `g2a-pos-core/includes/Database/WholesalerRepository.php:22-33` — `ORDER BY id ASC LIMIT 1` on `provider_code`, and `LipseysProvider::CODE` is one fixed literal for both accounts, so `WholesalerImportBridge::resolve_wholesaler_id()` always resolves to the same row once one account exists. (b) `wc_category_id` mapping column exists (`Migrator.php:449`, `WholesalerCategoryRepository.php:61`) but is never read or applied in `VendorProductPromoter.php` — zero repo-wide hits for `wp_set_object_terms`/`set_category_ids` in this promotion path. (c) Images hotlinked to `lipseyscloud.com`, never mirrored locally (`LipseysImageUrls.php`). | Account-aware resolution (key by provider_code **+** account_number, not provider_code alone); apply `wc_category_id` during promotion; mirror images locally | **Critical** | See full data-flow and fix plan in `08-LIPSEYS-INTEGRATION-AUDIT.md` | Configure both Lipsey's accounts with distinct account numbers; run an import for each; confirm products from BOTH appear in WooCommerce with correct, distinct categories and locally-hosted images |
| 7, 15, 17 | Otter Text + Otter Waiver still in use; chatbot liked, cancel Otter Text ASAP but need 100% confirmation; start closing Otter Waiver | **Split finding — Otter Text: cannot verify from source; Otter Waiver: strong import exists with one precise defect** | `guns2ammo/inc/ottertext-cleanup.php` explicitly documents Otter Text as an externally-injected script never part of this repo; no visitor-facing chat widget/REST route found anywhere. Otter Waiver importer (`class-waiver-import.php`) is idempotent, dedupes, protects PDFs — but `stats['members_matched']` (`:196-205`) can overcount: it increments whenever a WP-user email match is found, even when `stamp_member()` (`:452-462`) silently no-ops because no Memberistic person record exists for that email. | Live identification of the chatbot's actual technical implementation (cannot be done from source); a per-record unmatched/unstamped report from a real (non-dry-run) import | **Critical** (Otter Text — blocks a vendor-cancellation decision); **High** (Otter Waiver — fixable before next import run) | Do NOT cancel Otter Text until its implementation is found live (check page source, Plugins list, header/footer-script injectors, GTM). Fix the `stamp_member()` silent-failure before the next/final Otter Waiver import; add a distinct "matched user, no person record" counter | Otter Text: view-source the live site and grep for the actual embed; Otter Waiver: re-run import against the current full export with the fix, confirm `members_matched` count equals actual `waiver_status='signed'` row count in `memberistic_people` |
| 8 | Logins better, but older customers still struggle | **Cache fix confirmed real in code; UX improvements not yet built** | `guns2ammo/inc/login.php:51-60` (`g2a_login_prevent_cache()`) correctly sets `nocache_headers()` + `DONOTCACHEPAGE`/`DONOTCACHEOBJECT`/`DONOTCACHEDB` on login/account templates, plus a built-in self-diagnostic verdict string. Whether Cloudflare's edge actually honors these origin headers (vs. an overriding "Cache Everything" page rule) cannot be verified from source. | Passwordless/magic-link login; staff "send login link" tool; larger touch targets; plain-language errors | Medium (code fix is real) / **Live-verification-required** (Cloudflare edge behavior) | Verify Cloudflare page rules/cache level for `/login/` and `/account/` respect origin no-cache headers; then build the passwordless-login UX improvements as a separate initiative | Log in from a private/incognito window immediately after a fresh Cloudflare cache purge and confirm no stale logged-out redirect |
| 9, 11 | Counter/walkthrough process not detailed; not confident to teach employees range check-in | **Checklist delivered (`docs/RANGE_1.MD` / `RANGE_COUNTER_CHECKLIST.md`); underlying mechanics independently verified this session** | QR self-check-in (`class-range-controller.php`) and booking check-in (`class-bookings-controller.php`) both re-derive waiver satisfaction from `Waivers_Archive::has_on_file()` via the `g2ab_waiver_satisfied` filter (`class-waiver-booking-bridge.php`) — robust, does not depend on the potentially-stale `People_Repository.waiver_status` field. | A live walkthrough confirming the checklist matches actual on-screen behavior (explicitly flagged as outstanding in the checklist's own header) | Medium | Run the live walkthrough named in the checklist itself before training any employee from it | Manager + one staff member complete a full shift simulation (member, guest, walk-in, expired waiver, unpaid booking) against the live system using only the checklist |
| 10 | Customer contacts going to Formistic, working well | **Confirmed working as designed** | Formistic correctly reads `g2a_biz()` for footer/sender identity; WPistic Contact Form was formally retired in favor of Formistic (git history `e214285`) | None identified | — | No action needed beyond normal monitoring | — |
| 12, 20 | Merchant-service decision open; want Stripe gone, replaced with something manageable in-house | **Confirmed: Booking Engine already multi-gateway; Memberistic is Stripe-only** | `g2a-booking-engine/includes/payments/` has `class-fortis.php`, `class-paypal.php`, `class-authnet.php` alongside Stripe and pay-in-store. Memberistic's `includes/payments/class-stripe-service.php` has no gateway abstraction — Stripe API calls are direct, not behind an interface. | `MembershipGatewayInterface` abstraction; a second gateway adapter; a controlled migration plan | High (not urgent — do not rip out Stripe first) | Build the abstraction, prove a second gateway on new signups only, retire Stripe by attrition — see `07-PAYMENTS-AND-MERCHANT-SERVICE-PLAN.md` | A new membership signup completes full lifecycle (charge → active → renewal → cancel) through the new gateway with zero Stripe involvement, while existing Stripe subscriptions continue uninterrupted |
| 13 | Live site, no chance to test first, solving problems as found | **Confirmed operational risk — matches prior finding** | No staging environment artifact found in-repo; `scripts/` contains only `build-release-zips.sh` and `fetch-fonts.sh`, no deploy/staging automation | A staging clone; a deploy pipeline; the production manifest described in `01-SYSTEM-INVENTORY.md` | **Critical** (root cause enabler for nearly every other finding) | Phase 0 — stand up staging before further hotfixes | A code change is deployed to staging, verified, then promoted to production via a repeatable process |
| 14, 22 | SEO good, summer sales down, lots of range use, little retail; want email/SMS/social push | **Confirmed: plumbing exists, segmentation/campaign logic does not** | Formistic (contacts, consent) + Messageistic (SMS, consent, campaign repositories) provide infrastructure; no evidence of built segmentation logic for range-visitor-to-retail, lapsed-member, or the other named personas | Segmentation queries; campaign templates/automation for the named personas | Medium (marketing opportunity, not a defect) | See `13-SEO-CRO-REVENUE-GROWTH.md` for the specific campaign recommendations | A defined segment (e.g., "checked in ≥3× in 60 days, zero WooCommerce orders") can be queried and exported for a campaign |
| 18 | Need cleanup of contacts + duplicate check | **Confirmed missing — no reconciliation tool exists** | No database-wide dedup tool found; Formistic's own dedupe only prevents double-click submissions, not cross-system duplicate people | Customer Identity Reconciliation Tool (detect, never auto-merge) | High | See `06-DATA-INTEGRITY-AND-RECONCILIATION.md` for design | A deliberately-seeded duplicate (same email, two person records) is correctly flagged for staff review and, on approval, merged with full audit trail |
| 19 | Some members have wrong people attached to their memberships | **Confirmed code defect — precise, schema-level root cause found** | `memberistic_people.email` is a plain `KEY`, not `UNIQUE` (`class-schema.php:79,93`); `People_Repository::create()` (`:40-61`) never checks for an existing person by email before inserting; `get_by_email()` (`:96-106`) does `ORDER BY id DESC LIMIT 1`, silently resolving to the most recently created duplicate on every lookup (including the waiver-stamping path) | Uniqueness enforcement or an explicit merge/dedupe step at creation time; a membership-integrity audit report | **Critical** | See `06-DATA-INTEGRITY-AND-RECONCILIATION.md` for the audit-rule design | Attempt to create two person records with the same email under different memberships in a staging environment; confirm the system flags rather than silently allows it, after the fix |
| 21 | Waivers from Otter Waiver need cross-checked, shown as "signed" on the right account | **Partially confirmed — archive-level import is solid; person-record stamping has a silent-failure gap (same defect as #7/17)** | Check-in itself is robust (re-derives from `Waivers_Archive` directly, per #9/11 above). The **account-page display** most likely reads `People_Repository.waiver_status`, which the `stamp_member()` bug can leave stale even when the archive correctly holds the signed waiver and check-in works. | Fix `stamp_member()`'s silent no-op; add an unmatched/unstamped report from a real import run | High | Same fix as item 7/17's Otter Waiver defect | After the fix, every `memberistic_people` row with a matching `memberistic_waivers_archive` current record shows `waiver_status = 'signed'` |

**Every line of the client's message is accounted for above.** Where this audit's finding differs materially from the client's own prior status document (`docs/CLIENT~1.MD`), the difference is: this audit identifies the **exact mechanism** (specific line, specific ordering, specific missing index) rather than describing the symptom or a "most likely cause." That precision is what makes items 1, 3, 6, and 19 directly actionable as small, isolated code changes rather than open-ended investigations.
