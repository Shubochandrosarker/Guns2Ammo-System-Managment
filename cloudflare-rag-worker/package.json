{
  "name": "g2a-rag-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Guns2Ammo POS RAG service: Workers AI (bge-m3) embeddings + Vectorize storage behind a bearer-auth JSON API.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.20.0"
  }
}
